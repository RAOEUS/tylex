# Tylex user configuration file
# This file is copied to ~/.config/tylex/config.sh on first install.
# You can edit that file to customize settings.

# Delay between each typed character in milliseconds.
# Lower values are faster but may be less reliable on some systems.
KEY_DELAY_MS=20

# Keyword to trigger the "Open config directory" action from the launcher.
CONFIG_KEYWORD="config"

# Keyword to trigger the "Open expansions directory" action.
DATA_KEYWORD="data"

# Optional: Override the menu launcher.
# Examples:
# LAUNCHER="rofi -dmenu -i -matching fuzzy"
# LAUNCHER="dmenu -i -l 10"
# Leave blank to auto-detect 'rofi' or 'dmenu'.
LAUNCHER=""

